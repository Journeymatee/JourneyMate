/**
 * About-page `BackLink` — kept as a thin re-export so existing
 * imports (`'../components/about'`) continue to resolve. The
 * implementation now lives in the shared `ui` design-system
 * primitive: see `components/ui/BackLink.jsx`.
 */
export { default } from '../ui/BackLink'
